// Use to write functions to query store
// outside of React components

/*
import { store } from '.';

export const getUser = () => {
    const { app: { user } } = store.getState();
    return user;
};
*/
